import { Grid } from "@mui/material";
import { styled } from "@mui/system";

export const StyledContainer = styled(Grid)({
  padding: "1rem",
});

export const StyledCategory = styled(Grid)({});

export const StyledProducts = styled(Grid)({
  margin: "1.5rem 0",
});

export const StyledSearchSort = styled(Grid)({});

export const StyledSearch = styled(Grid)({});

export const StyledSort = styled(Grid)({ textAlign: "right" });
